const Blog = () => {
  return (
    <main className="page">
      <section className="page__hero">
        <h1>Blog</h1>
        <p>Insights, updates, and stories from the IMOVA team.</p>
      </section>
    </main>
  );
};

export default Blog;
